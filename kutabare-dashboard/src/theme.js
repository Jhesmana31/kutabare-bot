import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#7b1fa2',  // violet-ish
    },
    secondary: {
      main: '#f50057', // pink-ish
    },
  },
});

export default theme;
